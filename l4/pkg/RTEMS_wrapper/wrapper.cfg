-- vim:set ft=lua;

-- This is a configuration file for the RTEMS Wrapper

require("L4")

local l = L4.default_loader;

local my_caps = {
  mem = L4.Env.mem_alloc;
  }

l:start( {caps = my_caps, log= {"wrapper", "green"} },
		  "rom/RTEMS_wrapper rom/myHello.exe");
